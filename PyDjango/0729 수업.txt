세션 ( session ) : 로그인, 장바구니...

http 정의문서 ( 단락형 ) 단락형 : 요청을 받고 응답을 한 후 클라이언트와 서버의 연결이 끊어짐

http: 하이퍼텍스트전송 프로토콜

web (client (web 브라우져) -> server(장고) ) : 리퀘스트

web (client (web 브라우져) <- server(장고) ) : re·sponse

세션은 클라이언트가 요청 하면 과부하를 방지하기 위해 메모리 쿠키를 사용해 서버에 클라이언트의 고유한 id를 부여해준다.

세션 타임아웃 : 마지막 요청 이후지정된 시간동안 추가 요청이 없으면 서버에서 연결을 끊어버린다.(짧을수록 보안은 올라간다)
